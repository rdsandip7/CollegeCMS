@model PresentationLayer.Models.EmployeeViewModel
@{
    ViewBag.Title = "SaveEmployee";
    }
    <!-- BOOTSTRAP STYLES-->
    <link href="~/assets/css/bootstrap.css" rel="stylesheet" />
    <!-- FONTAWESOME STYLES-->
    <link href="~/assets/css/font-awesome.css" rel="stylesheet" />
    <!-- CUSTOM STYLES-->
    <link href="~/assets/css/custom.css" rel="stylesheet" />
    <!-- GOOGLE FONTS-->
    <link href='http://fonts.googleapis.com/css?family=Open+Sans' rel='stylesheet' type='text/css' />
<link href="~/assets/css/registration.css" rel="stylesheet" />
    <div id="wrapper">
     <div>
       @Html.Partial("~/Views/Shared/navigator.cshtml")                
     </div>
     <div id="page-wrapper">
            <div id="page-inner">
<table>
    <tbody>
        <th><h1>Following Department Data Has Been Saved </h1></th>
        <tr>
            <td> @Html.LabelFor(x => x.FirstName) :</td>
            <td>@Model.FirstName</td>
        </tr>
        <tr>
            <td> @Html.LabelFor(x => x.MiddleName) :</td>
            <td>@Model.MiddleName</td>
        </tr>
        <tr>
            <td>@Html.LabelFor(x=>x.LastName) :</td>
            <td>@Model.LastName</td>
        </tr>
        <tr>
            <td>@Html.LabelFor(x=>x.Department) :</td>
            <td>@Model.Department</td>
        </tr>
        <tr>
            <td>@Html.LabelFor(x=>x.Phone) :</td>
            <td>@Model.Phone</td>
        </tr>
        <tr>
            <td>@Html.LabelFor(x=>x.Address) :</td>
            <td>@Model.Address</td>
        </tr>
        <tr>
            <td>@Html.LabelFor(x=>x.Position) :</td>
            <td>@Model.Position</td>
        </tr>
        <tr>
            <td>@Html.LabelFor(x=>x.Gender) :</td>
            <td>@Model.Gender</td>
        </tr>
        <tr>
            <td>@Html.LabelFor(x=>x.HiredDate) :</td>
            <td>@Model.HiredDate</td>
        </tr>
        <tr>
            <td>@Html.LabelFor(x=>x.ResignationDate) :</td>
            <td>@Model.ResignationDate</td>
        </tr>
    </tbody>
</table>
                </div>
         </div>
        </div>
<!-- JQUERY SCRIPTS -->
    <script src="~/assets/js/jquery-1.10.2.js"></script>
    <!-- BOOTSTRAP SCRIPTS -->
    <script src="~/assets/js/bootstrap.min.js"></script>
    <!-- METISMENU SCRIPTS -->
    <script src="~/assets/js/jquery.metisMenu.js"></script>
    <!-- CUSTOM SCRIPTS -->
    <script src="~/assets/js/custom.js"></script>